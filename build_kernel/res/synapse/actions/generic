if [ -e /su/xbin/busybox ]; then
	BB=/su/xbin/busybox;
elif [ -e /system/xbin/busybox ]; then
	BB=/system/xbin/busybox;
fi;

if [ -f "$1" ]; then
	if [[ ! -z "$2" ]]; then
		$BB echo "$2" > "$1"
	fi;

	$BB echo "$($BB cat "$1")";
fi;
